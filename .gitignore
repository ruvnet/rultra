/target
/target-bookworm
**/*.rs.bk
*.rvf.lock
.env

# Local agent tooling state — machine-specific, regenerated, not part of the project
.claude-flow/
.swarm/
.hive-mind/

# Never commit anything that could carry a token
ui.env
*.env
!.env.example
